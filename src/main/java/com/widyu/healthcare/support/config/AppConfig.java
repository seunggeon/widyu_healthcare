package com.widyu.healthcare.support.config;

import org.springframework.stereotype.Component;

@Component
public class AppConfig {
    public static long GOAL_POINT = 10; // 목표 달성 포인트 (고정값)
    public static long REWARD_POINT = 2; // 리워드 포인트
}
