<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.widyu.healthcare.mapper.RewardMapper">

    <insert id="insertReward" parameterType="com.widyu.healthcare.dto.reward.RewardDTO">
        INSERT INTO reward (userIdx, url, description) VALUES (#{userIdx}, #{url}, #{description})
    </insert>

    <update id="updateReward" parameterType="com.widyu.healthcare.dto.reward.RewardDTO">
        UPDATE reward
        SET url = #{url}, description = #{description}
        WHERE rewardIdx = #{rewardIdx}
    </update>

    <update id="updateRewardUrl" parameterType="com.widyu.healthcare.dto.reward.RewardDTO">
        UPDATE reward
        SET url = #{url}, description = #{description}
        WHERE rewardIdx = #{rewardIdx}
    </update>

    <update id="updateGoalStatusUrl" parameterType="com.widyu.healthcare.dto.reward.RewardDTO">
        UPDATE reward
        SET url = #{url}
        WHERE rewardIdx = #{rewardIdx}
    </update>

    <select id="getUrlByRewardId" parameterType="java.lang.Long" resultType="String">
        SELECT url FROM reward
        WHERE rewardIdx = #{rewardIdx}
    </select>

</mapper>