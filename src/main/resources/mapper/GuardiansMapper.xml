<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.widyu.healthcare.mapper.GuardiansMapper">

    <select id="checkId" resultType="int">
        select count(id)
        from users
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.widyu.healthcare.dto.users.UsersDTO">
        INSERT INTO users(id, password)
        VALUES(#{id}, #{password})
    </insert>

    <select id="findByIdAndPassword" resultType="com.widyu.healthcare.dto.users.UsersDTO">
        SELECT *
        FROM users
        WHERE id = #{id}
          AND password = #{password}
          AND status != 'DELETE'
    </select>

    <select id="findSeniorsById" resultType="com.widyu.healthcare.dto.users.UsersDTO">
        SELECT seniorIdx, description
        FROM relations
        WHERE guardianIdx = #{userIdx}
        AND status != 'DELETE'
        LIMIT 10
    </select>


</mapper>
